# Nutrition Tracking App

Мобильное веб-приложение для подсчёта КБЖУ (калории, белки, жиры, углеводы).

## Технологии

- **React 18** + **TypeScript** — UI библиотека с типизацией
- **Vite** — быстрый сборщик
- **Tailwind CSS** — утилитарный CSS фреймворк
- **Supabase** — PostgreSQL база данных и backend
- **Zustand** — легковесное управление состоянием
- **React Hook Form** + **Zod** — формы с валидацией
- **React Router** — маршрутизация

## Быстрый старт

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка Supabase

1. Создайте файл `.env` в корне проекта (скопируйте из `.env.example`)
2. Заполните переменные окружения:
```env
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
```

3. Настройте базу данных:
   - Откройте [Supabase Dashboard](https://app.supabase.com)
   - Перейдите в SQL Editor
   - Выполните SQL скрипт из файла `supabase-schema.sql`

### 3. Запуск приложения

```bash
# Development сервер
npm run dev

# Production сборка
npm run build

# Preview production сборки
npm run preview
```

## Импорт в GitHub и запуск на Vercel

Пошаговая инструкция: **[GITHUB_VERCEL.md](./GITHUB_VERCEL.md)** — как выложить проект в GitHub и подключить к [Vercel](https://vercel.com/).

Кратко: создать репозиторий на [github.com/new](https://github.com/new) → залить код (через сайт или `git push`) → в [Vercel](https://vercel.com) выбрать **Add New → Project** и указать репозиторий → Deploy.

## Структура проекта

```
src/
├── app/                    # Конфигурация приложения
│   ├── router.tsx         # Настройка роутинга
│   └── providers.tsx      # Провайдеры
├── features/              # Функциональные модули
│   ├── nutrition/         # Модуль питания
│   │   ├── components/    # UI компоненты
│   │   ├── services/      # API сервисы
│   │   ├── store/         # Zustand store
│   │   ├── types/         # TypeScript типы
│   │   └── utils/         # Утилиты модуля
│   └── splash/            # Splash Screen
├── shared/                # Общие компоненты и утилиты
│   ├── lib/               # Библиотеки (Supabase client)
│   ├── types/             # Общие типы
│   └── utils/             # Общие утилиты
└── styles/                # Глобальные стили
```

## Функциональность

### Текущая версия (v1.0)

- ✅ Splash Screen с анимацией
- ✅ Главный экран с отображением дневной статистики КБЖУ
- ✅ Прогресс-бары для макронутриентов (белки, углеводы, жиры)
- ✅ Drawer с формой добавления питания
- ✅ Валидация форм (Zod)
- ✅ Интеграция с Supabase
- ✅ Обработка ошибок и состояний загрузки
- ✅ Мобильная оптимизация

### Планируется (v2.0+)

- 🔜 Авторизация и регистрация
- 🔜 Сканирование бар-кодов
- 🔜 База данных продуктов (ГОСТ)
- 🔜 История питания
- 🔜 Настройка целевых значений КБЖУ
- 🔜 Статистика и графики

## Архитектура

Приложение построено на принципах **Feature-based архитектуры**:

- Каждый модуль изолирован в папке `features/`
- Модули зависят только от `shared/`, но не друг от друга
- Бизнес-логика вынесена в `services/`
- UI компоненты только отображают данные
- Типизация на всех уровнях (TypeScript)

## Безопасность

- **Row Level Security (RLS)** — подготовлено для будущей версии с авторизацией
- **Input Validation** — валидация на клиенте (Zod) и сервере
- **SQL Injection Protection** — использование Supabase Client
- **Environment Variables** — секреты в `.env` файле

## Разработка

### Добавление новой фичи

1. Создайте папку в `features/`
2. Изолируйте логику модуля
3. Используйте общие компоненты из `shared/`
4. Добавьте типы и сервисы
5. Настройте роутинг при необходимости

### Стилизация

Используются точные цвета из дизайна:
- Splash Screen фон: `#1A3B14`
- Splash Screen текст: `#8CE750`
- Protein прогресс-бар: `#FFD700` (желтый)
- Carbs прогресс-бар: `#FFA500` (оранжевый)
- Fat прогресс-бар: `#8A2BE2` (фиолетовый)
- Кнопка добавления: `#90EE90` (светло-зеленый)
